{
  "experiment": "e00_smoke_test",
  "description": "Ladder enumeration and controlled-variable audit for the R3 control. The flatness comparison against SmolLM2 and Qwen3-4B is only meaningful if this server compiled the same buckets, and max_model_len is 4096 here rather than 8192, so the ladder MUST be read from the log rather than assumed.",
  "model": "TinyLlama/TinyLlama-1.1B-Chat-v1.0",
  "tpu_type": "v5litepod-4",
  "warmup_log_path": "/tmp/vllm_warmup.log",
  "controlled": {
    "enable_prefix_caching": false,
    "enable_chunked_prefill": true,
    "max_num_batched_tokens": 8192,
    "tensor_parallel_size": 4,
    "speculative_model": null,
    "kv_cache_dtype": "auto",
    "VLLM_TPU_BUCKET_PADDING_GAP": "",
    "max_model_len": 2048,
    "XLA_FLAGS": "",
    "ATTN_BUCKETIZED_NUM_REQS": false
  },
  "dimension": "none"
}
